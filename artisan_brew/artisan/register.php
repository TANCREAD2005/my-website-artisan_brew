<?php
require "includes/db.php";if(session_status()===PHP_SESSION_NONE)session_start();$pageTitle="Register | Artisan Brew Co.";$error="";
if($_SERVER['REQUEST_METHOD']==='POST'){$name=trim($_POST['name']);$email=trim($_POST['email']);$pass=$_POST['password'];if(strlen($pass)<6)$error="Password must be at least 6 characters.";else{$hash=password_hash($pass,PASSWORD_DEFAULT);$s=$conn->prepare("INSERT INTO users(name,email,password) VALUES(?,?,?)");$s->bind_param("sss",$name,$email,$hash);if($s->execute()){$_SESSION['user']=['id'=>$s->insert_id,'name'=>$name,'role'=>'customer'];header("Location:index.php");exit;}$error="Email is already registered.";}}require "includes/header.php";?>
<section class="form-page"><div class="form-card"><p class="eyebrow">WELCOME</p><h1>Create <em>Account.</em></h1><?php if($error):?><div class="alert"><?=$error?></div><?php endif;?><form method="post"><label>Full Name<input name="name" required></label><label>Email<input type="email" name="email" required></label><label>Password<input type="password" name="password" required></label><button class="btn dark">Register</button></form><p class="form-note">Already registered? <a href="login.php">Login</a>.</p></div></section><?php require "includes/footer.php";?>